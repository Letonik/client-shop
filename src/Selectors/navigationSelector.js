export const getSectors = (state) => {
    return state.navigation.sectors;
}

export const getCategories = (state) => {
    return state.navigation.categories;
}

export const getProducts = (state) => {
    return state.navigation.products;
}

export const getBrands = (state) => {
    return state.navigation.brands;
}