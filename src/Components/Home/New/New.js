import React from 'react';
import {Container} from "react-bootstrap";
import style from "./New.module.scss";

const New = () => {
    return (
        <Container fluid className={style.contain}>
            НОВИНКИ
        </Container>
    );
};

export default New;